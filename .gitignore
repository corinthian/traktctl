# Build / release output
/dist/

# Local credential fallbacks (dev only; real creds live in env or macOS Keychain)
/config.toml
/tokens.json

# macOS
.DS_Store

# Installed-skill local files (never in the repo)
.claude/skills/traktctl/PERSONAL.md
.claude/skills/traktctl/INCIDENTS.md
